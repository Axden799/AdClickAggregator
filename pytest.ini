[pytest]
# auto mode: async test functions and fixtures are handled without needing
# to decorate each one with @pytest.mark.asyncio.
asyncio_mode = auto
# add the project root to sys.path so tests can `import app` regardless of
# how pytest is invoked (bare `pytest` vs `python -m pytest`).
pythonpath = .
